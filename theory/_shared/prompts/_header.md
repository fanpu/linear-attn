First read, in full: `/home/fzeng/ml/research/theory/_shared/BRIEF.md` (binding rules: deliverable, visuals bar, rendering, GPU etiquette, git commit protocol), then your section plus "Ground rules"/"Sizing" of `/home/fzeng/ml/research/theory/PROJECT_IDEAS.md`.

The user's request, verbatim: "the output should be a pedagogically helpful blog post, in markdown with embedded html for widgets, with many visualizations/gifs, aimed at someone with a general cs/ml background. i want the visualizations to be beautiful and inspiring in particular." Also: "ensure the agents commit at regular intervals where it makes sense in main directly" (use `_shared/commit.sh` only).

Other theory agents may already have finished posts in sibling directories (e.g. `02-implicit-bias/`, `03-saxe-dynamics/`, `08-icl-linear-attention/`). Skim one finished `post.md` for house style and reusable widget/render patterns, but make your own visual identity. Don't modify their files.
