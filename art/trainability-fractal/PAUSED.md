# PAUSED (session limit, second stop)
- Done: float64 zoom keyframes 10^0..10^2.5 (cache/zoom_zoomA kf0-5, half-decade) + 10^3.5, 10^4.5 (cache/zoom_zoomB kf0-1, decade); 0 of boundary px flip under a 1-ulp nudge at every NN level; D rises 1.20 -> 1.66. Native 1024^2 float32 hero computed (cache/windows/hero_overview_tanh_1024_f32.npz; f32 vs f64 agree on 99.8% of px, 20% of boundary px). 2x/4x resolution blocks (res_kf5_block_64/128). Spectral diptych B, Liu deflation, README body drafted (placeholders <!-- HERO/ZOOM/DTABLE/FLIPS/RES/NKF/HEROT -->), merge_zoom.py, palette-variant hero option.
- Next: resume zoomB to 10^6.5 (kf2-3), `python merge_zoom.py`, `python render_zoom.py zoomAB --plates --video`, `python render_hero.py hero_overview_tanh_1024_f32 overview_tanh` (+ `--style aurora_ember`, `--style indigo_madder`), `python verify.py zoomAB null_quadratic liu`, analyse res blocks, fill README placeholders, link check, commit.
- Resume: `cd art/trainability-fractal && ../_shared/gpu_run.sh bash logs/zoomB_run.sh >> logs/zoomB.log 2>&1 &` (resumes from cached keyframes).
