# CS 312: Deep Learning Alchemy (Stanford, Fall 2026): site snapshot

Source: https://deep-learning-alchemy.github.io/ , fetched 2026-09-19. Raw HTML is next to this file.
Only repo in their GitHub org on that date: `deep-learning-alchemy/deep-learning-alchemy.github.io` (the website).
No handouts, lecture notes, or quizzes were public yet. Re-check at the start of every unit.

Instructors: Tatsunori Hashimoto, Suhas Kotha. CAs: Kaiyue Wen, Tristan Thrush. Tue/Thu 1:30-2:50pm. 5 units.
Site template adapted from CS336.

## Philosophy

Learn deep learning through experiments. (1) Hands-on experiment design to build intuition about empirical
phenomena; (2) understanding is assessed by **prediction quizzes**: forecast the outcome of an experiment before
seeing the result.

## Unit loop (their "example schedule")

- At the intro meeting and after each quiz: the next unit's assignment is released.
  - The assignment "scopes out empirical phenomena that students will best understand through designing clean experiments."
  - Students eventually submit a report with plots/tables explaining the experiments they ran.
- One or two class meetings: lectures explain common intuitions for the unit.
- Once a week: 30 minute tutorial, three other students + one staff. Staff asks what experiments each student is
  running and covers practice problems that help for the quiz. Doubles as group office hours.
- The day before the quiz: report due. Not graded; only useful for partial credit on the quiz. Reports may inspire quiz questions.
- End of unit: 40-minute experiment prediction quiz.
  - Each question corresponds to a thematic **held-out experiment run by the instructors**. The student gets a
    description of the experiment (including code diff) and predicts the outcome.
  - Questions span easy / medium / hard depending on how much extrapolation they need from the assignment suggestions.
  - Closed book, including the student's own report. Students can cite their report and get partial credit if the reasoning is sound.
  - Immediately after the quiz, instructors cover the answers and discuss mental models.

## Assignments (tentative on their site)

Assignments 1-4 study language model pre-training "at a small scale reflective of larger-scale phenomena".
Assignments 5-7 switch to DNA.

1. Basics: Hyperparameter tuning and scaling
2. Optimization 1: Hyperparameter invariants
3. Optimization 2: Sharp and flat basins
4. Architecture 1: Extending model capacity
5. Architecture 2: Stability across depth/time
6. Generalization: Data-efficient algorithms
7. Exotic: ???

## Grading

15% tutorial participation (2 absences allowed); 85% quizzes (best 5 of 7); 0% assignments.

## AI policy

"We view assignments as instrumental to improving human understanding. Therefore, we support AI usage on
assignments for implementation, visualization, mock quizzes, etc. However, since grades will be primarily
determined by quiz performance, we encourage students to be thoughtful about what AI usage enhances their ability
to digest phenomena (as measured by their ability to predict experiment outcomes)." All submitted experiments
must be run by the student.

## Compute

They assume a single rented B200 (~$5-7/h in March 2026; Modal sponsors).

## Their calendar (reference only; we do not follow these dates)

| Date | Event |
|---|---|
| Tue 9/22 | Intro lecture; A1 released (due 9/28) |
| Thu 9/24 | Basics lecture |
| Tue 9/29 | Basics quiz; A2 released (due 10/7) |
| Thu 10/1, Tue 10/6 | Optimization 1 lectures |
| Thu 10/8 | Optimization 1 quiz; A3 released (due 10/19) |
| Tue 10/13, Thu 10/15 | Optimization 2 lectures |
| Tue 10/20 | Optimization 2 quiz; A4 released (due 10/28) |
| Thu 10/22, Tue 10/27 | Architecture 1 lectures |
| Thu 10/29 | Architecture 1 quiz; A5 released (due 11/9) |
| Thu 11/5 | Architecture 2 lecture |
| Tue 11/10 | Architecture 2 quiz; A6 released (due 11/18) |
| Thu 11/12, Tue 11/17 | Generalization lectures |
| Thu 11/19 | Generalization quiz; A7 released (due 12/2) |
| Tue 12/1 | Exotic lecture |
| Thu 12/3 | Exotic quiz |

## Example assignment problem (verbatim)

> Start from the baseline depth 8 transformer that we pre-train on 614M tokens. This transformer (implementation
> provided below) features multiple components such as prenorm, attention, MLP, residuals, and positional
> embeddings. In this problem, we want to understand how much each component improves loss. Explore the following
> directions.
> - Perform leave-one-out ablations on the role of each component in our current model.
> - ...
> - ...
> - Understand the role of the residual connection. Can you come up with different schemes that improve the loss
>   benefit of the residual connection?

```python
class Block(nn.Module):
    def __init__(self, width, ...):
        super().__init__()
        self.attn_norm = RMSNorm(width)
        self.attn = Attention(width, ...)
        self.mlp_norm = RMSNorm(width)
        self.mlp = MLP(width, ...)
    def update(self, x):
        attn = self.attn(self.attn_norm(x))
        mlp = self.mlp(self.mlp_norm(x + attn))
        return attn + mlp
    def forward(self, x):
        return x + self.update(x)
class Transformer(nn.Module):
    def __init__(self, vocab_size, depth=8, ...):
        super().__init__()
        self.token_embed = nn.Embedding(vocab_size, ...)
        self.position_embed = nn.Embedding(...)
        self.blocks = nn.ModuleList(
            [Block(...) for _ in range(depth)]
        )
        self.final_norm = RMSNorm(...)
        self.lm_head = nn.Linear(..., vocab_size)
    def forward(self, tokens):
        positions = torch.arange(
            tokens.size(1), device=tokens.device
        )
        x = self.token_embed(tokens)
        x = x + self.position_embed(positions)
        for block in self.blocks:
            x = block(x)
        return self.lm_head(self.final_norm(x))
```

## Example quiz question, difficulty easy (verbatim)

> We train each of the following depth-8 transformer variants using the same training recipe and hyperparameters.
> Rank the variants from lowest to highest final validation loss. Use <, >, or = between the letters; use = when
> the losses are within 0.01.
> Baseline / No prenorm / No residual / No MLP / No attention / No positional embeddings

```diff
# No prenorm
@@ -10,4 +10,4 @@ class Block.update
 def update(self, x):
-    attn = self.attn(self.attn_norm(x))
+    attn = self.attn(x)
-    mlp = self.mlp(self.mlp_norm(x + attn))
+    mlp = self.mlp(x + attn)
     return attn + mlp

# No residual
@@ -16,2 +16,2 @@ class Block.forward
 def forward(self, x):
-    return x + self.update(x)
+    return self.update(x)

# No MLP
@@ -10,4 +10,3 @@ class Block.update
 def update(self, x):
     attn = self.attn(self.attn_norm(x))
-    mlp = self.mlp(self.mlp_norm(x + attn))
-    return attn + mlp
+    return attn

# No attention
@@ -10,4 +10,3 @@ class Block.update
 def update(self, x):
-    attn = self.attn(self.attn_norm(x))
-    mlp = self.mlp(self.mlp_norm(x + attn))
-    return attn + mlp
+    mlp = self.mlp(self.mlp_norm(x))
+    return mlp

# No positional embeddings
@@ -22,2 +22,1 @@ Transformer.__init__
         self.token_embed = nn.Embedding(vocab_size, ...)
-        self.position_embed = nn.Embedding(...)
@@ -30,8 +29,4 @@ Transformer.forward
     def forward(self, tokens):
-        positions = torch.arange(
-            tokens.size(1), device=tokens.device
-        )
         x = self.token_embed(tokens)
-        x = x + self.position_embed(positions)
         for block in self.blocks:
             x = block(x)
```

## Example quiz question, difficulty hard (verbatim)

> We train the default 8 layer transformer as well as a variant where instead of having the residual connection
> come from the previous layer, we take 0.5 contribution from the previous layer and 0.5 contribution from two
> layers before (except for the first layer). For each method, we take the best loss over six learning rates
> {1e-4, 3e-4, 1e-3, 3e-3, 1e-2, 3e-2}. What is your prediction of the loss difference between each method
> (L_mixed residual − L_default)?

```diff
@@ -34,3 +34,9 @@ Transformer.forward
 x_two_back = None
 for i, block in enumerate(self.blocks):
-    x = block(x)
+    update = block.update(x)
+    residual = (
+        x if i == 0
+        else 0.5 * x + 0.5 * x_two_back
+    )
+    x_two_back, x = x, residual + update
```

> Note: both of these questions are hard, do not get discouraged if you do not have intuition for them without
> running experiments from the assignment!
